# Columns declare key and label; rows are maps keyed by column key.
# Keep cells to a sentence. Long prose belongs in markdown above the table.
- type: table
  columns:
    - { key: approach, label: Approach }
    - { key: decides, label: How it decides }
    - { key: struggles, label: Where it struggles }
  rows:
    - { approach: Pattern matching, decides: Matches shapes in parsed source., struggles: Blind to whether the path is guarded. }
    - { approach: Database queries, decides: Runs queries over a code model., struggles: Blind where nobody wrote a query. }
    - { approach: Agent review, decides: Reads the path and reasons., struggles: Judgement is model-driven, not bit-identical. }
